import { createTheme } from "@mui/material";

const Theme = createTheme({
    palette: {
        navyBlue: {
            main: "#023b66",
            contrastText: "#fff"
        }
    },

    typography: {
    },
});

export default Theme;
